server:
  port: 8770
#配置搜索引擎的配置
zipkin:
  storage:
    type: elasticsearch
    StorageComponent: elasticsearch
    elasticsearch:
      cluster: elasticsearch
      max-requests: 3
      index: stu-zipkin-log
      index-shards: 3
      index-replicas: 1
      hosts: localhost:9200